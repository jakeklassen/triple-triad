providers = ["node"]

[phases.install]
cmds = ["npm install -g corepack", "corepack enable", "corepack prepare pnpm@7.17.0 --activate", "pnpm install"]

[phases.build]
cmd = "pnpm -r build"

# [start]
# cmd = "pnpm --filter backend-socketio start:prod"
